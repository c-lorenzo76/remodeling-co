import {Layout} from "@/pages/Layout.tsx";
import {Contact} from "@/components/contact";


export const ContactPage = () => {
    return (
        <Layout>
            <Contact />
        </Layout>
    )
}